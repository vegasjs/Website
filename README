------------
VegasJS site
------------

Prerequisites:

npm install express
npm install hbs
npm install Reston
npm install underscore

Instructions:

1. Add your meetup.up API key (get it here: http://www.meetup.com/meetup_api/key/) as an environment variable

export MEETUP_API_KEY=[meetup key]

2. node app.js
3. Profit!


